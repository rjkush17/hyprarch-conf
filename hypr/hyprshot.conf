# Save path
output_path = ~/Screenshots/

# Image format
format = png  # Best quality (use jpg for compression)

# Quality (only applies to jpeg, but still good to set)
jpeg_quality = 100

# Include cursor in screenshot
include_cursor = true

# HiDPI support (ensures full resolution)
scale = 1.0

